[project]
name = "nova-sonic-poc"
version = "0.1.0"
description = "Minimal POC for Nova Sonic 2 with WebRTC (LiveKit)"
requires-python = ">=3.11"
dependencies = [
    "livekit-agents",
    "livekit-plugins-aws",
    "aws-sdk-bedrock-runtime",
]

[tool.uv]
package = false
