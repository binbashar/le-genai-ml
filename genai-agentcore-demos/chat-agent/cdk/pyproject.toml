[project]
name = "chat-agent-cdk"
version = "0.1.0"
description = "CDK infrastructure for chat-agent"
requires-python = ">=3.13"
dependencies = [
    "aws-cdk-lib>=2.170.0",
    "boto3>=1.34.0",
    "constructs>=10.0.0",
]
