[project]
name = "market-trends-agent"
version = "0.1.0"
description = "Intelligent financial analysis agent using Amazon Bedrock AgentCore"

readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "langgraph",
    "langchain-aws",
    "langchain-core",
    "boto3",
    "playwright",
    "bedrock-agentcore",
    "bedrock-agentcore-starter-toolkit",
    "rich",
    "aws-opentelemetry-distro>=0.12.1",
    "aiohttp",
    "python-dotenv>=1.1.1",
    "aws-cdk-lib>=2.221.0",
]

[project.optional-dependencies]
dev = [
    "pytest",
    "black",
    "flake8",
    "mypy",
    "aws-cdk-lib",
    "constructs",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.uv]
dev-dependencies = [
    "pytest>=7.0.0",
    "black>=23.0.0",
    "flake8>=6.0.0",
    "mypy>=1.0.0",
]

[tool.black]
line-length = 88
target-version = ['py313']

[tool.mypy]
python_version = "3.13"
warn_return_any = true
warn_unused_configs = true
disallow_untyped_defs = true
